# Ar-Stream 论文方法 → 代码实现详细映射

---

## 一、系统建模（论文第3.1节）

### 方法1：流应用DAG拓扑模型

- **论文描述**：将流应用拓扑形式化定义为 `G = (V, E)`，V 为算子集合（含无状态/有状态算子），E 为有向边集合，数据流为无限元组序列 `{tuple_i = (ts, key, payload)}`
- **实现类**：`FaftTopologyLauncher` (L151-157) / `YSBTopologyLauncher` (L137-145)
- **实现方式**：用 `Map<String, List<String>>` 表示 DAG（key=算子ID，value=直接下游列表）。Launcher 启动时硬编码构建，通过 `conf.put("faft.dag", dag)` 注入 Storm Config，供各 Bolt 在运行时读取
- **代码位置**：
  ```
  FaftTopologyLauncher.java:151-157  — 构建 dag Map
  YSBTopologyLauncher.java:137-145   — YSB拓扑的dag构建
  FaftSinkBolt.java:120-125          — prepare()中从Config读取dag
  ```

### 方法2：算子状态模型

- **论文描述**：有状态算子 vi 在时刻 t 的内部状态记为 `state(vi, t)`，由输入数据流驱动的离散事件更新：`state(vi, t) = f(state(vi, t_prev), tuple)`
- **实现类**：`FaftCountBolt` / `WindowAggregationBolt` / `JoinBolt`
- **实现方式**：通过 `HashMap<String, Integer>` 在 Bolt 内部维护有状态计算的中间结果。论文中的"真实视图"和"近似视图"分别对应代码中的 `realCounts` 和 `approxCounts` 两个隔离的 HashMap
- **代码位置**：
  ```
  FaftCountBolt.java:24-25    — Map<String, Integer> realCounts, approxCounts
  FaftCountBolt.java:126-132  — 状态更新：realCounts.put(word, count) / approxCounts.put(word, count)
  WindowAggregationBolt.java:22-23 — 同结构的双轨状态
  JoinBolt.java:20            — Map<String, Long> joinState (Join算子状态)
  ```

### 方法3：故障恢复时序模型

- **论文描述**：故障于 `t_failure` 发生，恢复周期 = 故障检测延迟 + 调度启动延迟 + 状态重建延迟 + 数据重放延迟。Ar-Stream 通过近似备份跳过全量重放，总恢复延迟 `T_recovery = T_detect + T_schedule + T_pull`，恢复后存在结构性偏差 `Δstate`
- **实现类**：`FaftLatencyMonitor` (ZK 记录时间戳) + `DefaultLatencyMonitor` (基线对比)
- **实现方式**：ChaosBolt 触发故障时调用 `recordFailure()` 写 ZK 时间戳；FaftCountBolt/WindowAggregationBolt 恢复后调用 `checkAndRecordRecovery()` 写入恢复时间戳，两个时间戳的差值即 RTO
- **代码位置**：
  ```
  FaftLatencyMonitor.java:74-97    — recordFailure(): 写ZK /faft/experiment/failure_time
  FaftLatencyMonitor.java:101-128  — checkAndRecordRecovery(): 写ZK /faft/experiment/recovery_time，输出RTO
  DefaultLatencyMonitor.java:40-59 — 基线版本的failure记录(ZK路径 /default/experiment/)
  DefaultLatencyMonitor.java:61-90 — 基线版本的recovery记录
  ChaosBolt.java:78                — 故障注入时调用 FaftLatencyMonitor.recordFailure()
  FaftCountBolt.java:115           — 恢复完成后调用 FaftLatencyMonitor.checkAndRecordRecovery()
  WindowAggregationBolt.java:86    — YSB拓扑中的恢复记录
  ```

---

## 二、算子重要性度量（论文第3.2节）

### 方法4：下游传播度 O(v)（OutputImpact）

- **论文描述**：基于反向误差传播的模型。Sink 节点 O(s)=1，中间算子 `O(v) = Σ(δ × O(child))`，通过反向拓扑传播 + 衰减因子 δ 累加。考虑算子对所有下游可达路径的覆盖程度及传播中的衰减
- **公式**：公式(3-4)
- **衰减因子 δ**：配置项 `faft.impact-delta`，默认 0.9
- **实现类**：`importance/OutputImpact.java`
- **实现方法**：`compute(Map<String, List<String>> dag, Set<String> sinks, double delta)`
- **算法流程**：
  1. Sink 节点 O=1.0，其余初始化为 0
  2. 对 DAG 做拓扑排序后反转（从 Sink 向上游传播）
  3. 构建反向邻接表（子节点→父节点列表）
  4. 每轮迭代：父节点的 O += δ × 子节点的 O
- **代码位置**：`OutputImpact.java:17-36`

### 方法5：局部结构依赖 D(v)（UpDownStreamDependency）

- **论文描述**：量化算子在拓扑中的枢纽地位。入度决定汇聚上游的广度，出度决定级联广播的范围。`D(v) = ωin × 入度(v) + ωout × 出度(v)`
- **公式**：公式(3-5)
- **权重参数**：`faft.omega-in`（默认0.5）和 `faft.omega-out`（默认0.5）
- **实现类**：`importance/UpDownStreamDependency.java`
- **实现方法**：`compute(Map<String, List<String>> dag, double omegaIn, double omegaOut)`
- **算法流程**：
  1. 遍历 DAG 邻接表，统计每个节点的入度和出度
  2. 对每个节点 v：`score = ωin × indegree(v) + ωout × outdegree(v)`
- **代码位置**：`UpDownStreamDependency.java:17-41`

### 方法6：运行时开销 C(v)（ComputeComplexity）

- **论文描述**：综合 CPU 占用率、内存占用率和吞吐量三个维度评估算子的资源消耗水平。`C(v) = wCpu × cpu(v) + wMem × mem(v) + wTps × tps(v)`，其中 tps 经过归一化
- **公式**：公式(3-6)
- **默认权重**：wCpu=0.5, wMem=0.3, wTps=0.2
- **实现类**：`importance/ComputeComplexity.java`
- **实现方法**：`compute(double cpuUsage, double memUsage, double tps, double wCpu, double wMem, double wTps)`
- **实现方式**：简单的加权线性求和，一行代码
- **代码位置**：`ComputeComplexity.java:9-12`
- **运行时数据来源**（周期性重评估时采集）：
  ```
  ApproxBackupManager.java:49 — processedByOp (ConcurrentHashMap): 各算子处理的tuple计数，差分得TPS
  ApproxBackupManager.java:52 — cpuNanosByOp (ConcurrentHashMap): 各算子execute()累计纳秒耗时，差分得CPU占用
  ApproxBackupManager.java:55 — stateSizeByOp (ConcurrentHashMap): 各算子HashMap条目数快照，归一化得内存占用
  ```
  各 Bolt 在 execute() 的 finally 块中上报：
  ```
  FaftCountBolt.java:151-152 — reportExecuteNanos() + reportStateSize()
  WindowAggregationBolt.java:111-112 — 同上
  FaftSinkBolt.java:186-187 — 同上
  JoinBolt.java:73-74 — 同上
  ```

### 方法7：算子综合重要性评估 I(v)（NodeImportanceEvaluator）

- **论文描述**：三因子归一化后加权融合。先对 O(v)、D(v)、C(v) 分别做极差标准化（除以各自最大值），再用各算子专属的 (α, β, γ) 权重融合：`I(v) = α × Ô(v) + β × D̂(v) + γ × Ĉ(v)`。最后线性映射到采样率：`r(v) = rmin + (I(v)/maxI) × (rmax - rmin)`
- **公式**：公式(3-7)
- **权重配置**：`application.yml` 中 `faft.weights` 为每个算子配置专属 (α,β,γ)，无配置则使用全局默认值
- **实现类**：`importance/NodeImportanceEvaluator.java`
- **实现方法**：`evaluateAndAssignRatios(dag, sinks, opInfo, weightsMap, defaultWeights, delta, omegaIn, omegaOut, rmin, rmax)`
- **返回值**：`Result` 对象，包含 `O, D, C, I, R` 五个 Map 和 `maxI`
- **调用场景**：
  - 拓扑启动时（Launcher 中）：用静态 OperatorInfo 做初始评估
  - 运行时（ApproxBackupManager.startDynamicRebalance()）：每10秒用实时指标重算
- **代码位置**：
  ```
  NodeImportanceEvaluator.java:61-125 — evaluateAndAssignRatios()方法
  NodeImportanceEvaluator.java:127-135 — normalize()极差标准化
  FaftTopologyLauncher.java:189-194    — 启动时调用
  YSBTopologyLauncher.java:174-178      — YSB启动时调用
  ApproxBackupManager.java:451-453      — 运行时周期性重评估调用
  ```

---

## 三、近似误差级联传播（论文第3.3节）

### 方法8：近似备份策略

- **论文描述**：对每个到达元组进行一次独立伯努利试验，以采样率 r(v) 决定是否备份。备份到外部存储（Redis）的状态为无偏估计量。局部误差方差与吞吐量正相关、与采样率反相关
- **公式**：公式(3-8)(3-9)(3-10)(3-11)
- **实现类**：`manager/ApproxBackupManager.java`
- **实现方法**：`tryBackup(String operatorId, int taskId, String word, int count)` (L175-203)
- **算法流程**：
  1. `processedCount++` 并更新 `processedByOp`（用于TPS统计）
  2. 从 `ratioByOperator` 表查该算子的当前采样率 `r`
  3. `random.nextDouble() < r` → 命中采样
  4. 命中后：I/O计数 +1，将 `(word, count)` 以 Hash 结构写入 Redis key `FAFT:BACKUP:{operatorId}:{taskId}`，设置1小时过期
- **调用方**：
  ```
  FaftCountBolt.java:136          — 处理TYPE_APPROX元组后调用 tryBackup()
  WindowAggregationBolt.java:101  — YSB拓扑中调用 tryBackup()
  ```

### 方法9：无偏估计状态重建（Redis恢复）

- **论文描述**：故障后从轻量级备份中重建近似状态，采用无偏估计量还原。期望上 `E[approx_state] = real_state`，但引入估计方差
- **实现类**：`manager/ApproxBackupManager.java`
- **实现方法**：`getBackup(String operatorId, int taskId)` (L208-227)
- **算法流程**：
  1. 构造 Redis key: `FAFT:BACKUP:{operatorId}:{taskId}`
  2. `jedis.hgetAll(key)` 拉取全部字段
  3. 逐条反序列化为 `Map<String, Integer>`（value 字符串 parse 为 int）
- **调用方（崩溃恢复时）**：
  ```
  FaftCountBolt.java:103          — 收到FAFT_CRASH_SIGNAL后 approxCounts.clear() → getBackup() → putAll()
  WindowAggregationBolt.java:77   — YSB拓扑中的恢复
  ```

### 方法10：误差级联传播递推

- **论文描述**：上游局部方差沿有向边向下游传播并叠加。递推关系：`Var(v) = Σ[(∂fv/∂xi)² × Var(ui)] + Var_local(v)`，其中偏导数为误差传播放大系数
- **公式**：公式(3-12)(3-13)
- **说明**：此为理论模型，论文未将其编写为独立的可执行代码方法。其效果体现在两个方面：
  - `OutputImpact.compute()` 的反向传播衰减模型（δ 参数）体现了误差在拓扑中的衰减传播
  - `ApproxBackupManager.adjustByError()` 的闭环补偿机制在实践中对抗该级联效应
- **相关代码位置**：
  ```
  OutputImpact.java:17-36   — 反向拓扑传播（建模了误差从下游向上游的归因路径）
  ```

### 方法11：全局平均相对误差 MRE 提取

- **论文描述**：在 Sink 节点处，通过锚点键的真实值与近似值的平均相对误差衡量全局输出精度。`MRE = (1/N) × Σ|truth(k) - approx(k)| / truth(k)`
- **公式**：公式(3-14)（论文3.3.3节）+ 公式(4-1)（论文4.2.1节）
- **实现类**：`topology/FaftSinkBolt.java`
- **实现方法**：`calculateAndAdjustGlobalMRE()` (L192-217)
- **算法流程**（对应论文算法4-1）：
  1. 遍历 `globalRealView` 中每个锚点键
  2. 从 `globalApproxView` 获取对应近似值
  3. 若真实值 > 0：累加 `|real - approx| / real`
  4. `MRE = totalError / items`
  5. 调用 `backupManager.adjustByError(globalMRE, errorThreshold)` 触发闭环调节
- **触发频率**：每 10,000 条 tuple 触发一次（`LOG_WINDOW = 10000`）
- **代码位置**：`FaftSinkBolt.java:176-178`（counter 累加 + L192-217 计算逻辑）

---

## 四、自适应步长调节（论文第4.2节）

### 方法12：算法4-1 — 低开销全局误差分析

- **论文描述**：遍历真值表中的锚点键，计算每个锚点的相对误差并累加，最终求均值得到无偏全局 MRE。时间复杂度 O(N_anchors)，空间复杂度 O(1)
- **实现类**：`topology/FaftSinkBolt.java`
- **实现方法**：`calculateAndAdjustGlobalMRE()` (L192-217)
- **实现细节**：
  - 论文中"真实视图表" = `globalRealView` (HashMap)
  - 论文中"近似视图表" = `globalApproxView` (HashMap)
  - 论文中"有效锚点计数器c" = 局部变量 `items`
  - 论文中"相对误差累加器" = 局部变量 `totalError`
  - 论文中"除以零保护" = `if (items == 0) return;`
- **代码位置**：`FaftSinkBolt.java:192-217`

### 方法13：动态比例补偿方程

- **论文描述**：简化 PID，仅保留比例项。越限时输出非线性补偿步长：`Δstep = Kp × (Eobs - Emax) + baseStep`。Kp=2.5 为比例增益系数，baseStep=step 为基础偏置
- **公式**：公式(4-2)
- **实现类**：`manager/ApproxBackupManager.java`
- **实现方法**：`adjustByError()` 内的越限补偿区分支 (L273-278)
- **实现细节**：
  ```java
  double Kp = 2.5;                         // 比例增益系数
  double errorDiff = Eobs - Emax;          // 误差超量
  double dynamicStep = (Kp * errorDiff) + this.step; // 动态补偿步长
  ```
- **代码位置**：`ApproxBackupManager.java:273-278`

### 方法14：算法4-2 — 自适应步长调节（分段控制函数）

- **论文描述**：三区段分段控制。容忍度系数 λ=0.5，下界 `lower = λ × Emax`。越限补偿区：输出 `+dynamicStep`；稳态容忍区 [lower, Emax]：输出 0（防震荡）；资源释放区：输出 `-step`（固定负向步长）
- **公式**：公式(4-3)
- **实现类**：`manager/ApproxBackupManager.java`
- **实现方法**：`adjustByError(double Eobs, double Emax)` (L263-371)
- **算法流程**：
  1. `lower = 0.5 × Emax`（容忍区下界）
  2. `if (Eobs > Emax)` → 越限补偿区，分层递进上调（见方法15）
  3. `else if (Eobs < lower)` → 资源释放区，对 Bottom 40% 边缘算子执行固定步长下调
  4. 否则 → 稳态容忍区，不触发任何调整（隐式实现，步长=0）
- **代码位置**：`ApproxBackupManager.java:263-371`

---

## 五、算子差异化调控（论文第4.3节）

### 方法15：算法4-3 — 细粒度差异调控算法

- **论文描述**：将全局步长 `Δglobal` 按算子重要性进行非对称分配。补偿期（Δglobal > 0）：分层递进 — 关键层0-30% → 承载层31-70% → 边缘层71-100%，仅当前层全部触达 rmax 时才下探；回收期（Δglobal < 0）：仅降 Bottom 40% 边缘算子，不向上扩展。调整结果经 clamp() 约束至 [rmin, rmax]
- **实现类**：`manager/ApproxBackupManager.java`
- **实现方法**：`adjustByError()` (L263-371)，整体实现了算法4-2 + 算法4-3
- **补偿区分层递进实现** (L273-343)：
  1. 按重要性降序排列算子
  2. 计算层级边界：`tier1End = min(30%, total)`, `tier2End = min(70%, total)`
  3. 从 Tier1 开始遍历：若算子采样率未达 `rmax`，则 `newR = clamp(oldR + dynamicStep)` 并写入 `ratioByOperator`
  4. 若 Tier1 全满载 → 升级到 Tier2，打印日志；Tier2 也满载 → 升级到 Tier3
  5. 全部满载 → 打印饱和告警
- **回收区平滑降配实现** (L344-370)：
  1. 按重要性升序排列算子
  2. `downTarget = max(1, size × 0.4)`（论文中为 Bottom 40%，代码实现为40%）
  3. 遍历：若采样率未触及 `rmin`，则 `newR = clamp(oldR - step)`
  4. 达到 `downTarget` 后停止
- **采样边界约束 clamp()** (L238)：
  ```java
  private double clamp(double x) { return Math.max(rmin, Math.min(rmax, x)); }
  ```
- **代码位置**：`ApproxBackupManager.java:238` (clamp), `L263-371` (整体算法)

---

## 六、故障恢复机制（论文第4.4节）

### 方法16：算法4-4 — 低延迟状态重构算法（冷启动 + 灾后补偿）

- **论文描述**：边界状态的生命周期管理。冷启动期（T_now < T_init）：强制所有算子采样率 = rmax，屏蔽失真误差信号；灾后重构期（Eobs > Emax）：重新唤醒调度，调用算法4-2计算补偿步长 → 算法4-3计算新采样率矩阵 → 高频数据冲刷；平稳期：返回常规闭环调度结果
- **实现分散在多个位置**：

**冷启动部分**（论文4.4.1节）：
- **实现类**：`manager/ApproxBackupManager.java` 构造函数 (L88)
- **实现方式**：`this.currentRatio = rmax` — 初始采样率直接设为上限 1.0，确保启动初期全量备份。论文的"安全水位接管策略"通过将初始 `currentRatio` 设为 `rmax` 实现
- **代码位置**：`ApproxBackupManager.java:88-89`

**崩溃恢复（状态清空 + Redis恢复）**（论文式(3-2)，式(3-3)）：
- **实现类**：`FaftCountBolt` / `WindowAggregationBolt`
- **算法流程**：
  1. 检测 `"FAFT_CRASH_SIGNAL"` → 仅处理 TYPE_APPROX
  2. `approxCounts.clear()` — 模拟内存状态丢失
  3. `backupManager.getBackup(componentId, taskId)` — 从 Redis 拉取备份
  4. `approxCounts.putAll(backup)` — 恢复近似状态
  5. `FaftLatencyMonitor.checkAndRecordRecovery()` — 记录 RTO
- **代码位置**：
  ```
  FaftCountBolt.java:91-115         — 崩溃信号处理 + Redis恢复
  WindowAggregationBolt.java:71-89  — YSB拓扑中的同逻辑
  ```

**灾后误差自动补偿（闭环）**（论文4.4.2节）：
- 该机制非独立方法，而是闭环自动完成：
  - 脏状态 → MRE 上升 → 触发越限补偿区 → 动态比例步长放大 → 分层递进上调 → 高频数据冲刷 → MRE 回落 → 触发资源释放区 → 逐步降配
- **触发链代码路径**：
  ```
  FaftSinkBolt.calculateAndAdjustGlobalMRE() → L215
    → ApproxBackupManager.adjustByError()   → L263-371
      → ratioByOperator 更新
        → 各 Bolt 下次 tryBackup() 时自动使用新采样率
  ```

**周期性重评估（动态 I→r 重算）**（论文4.5节）：
- **实现类**：`manager/ApproxBackupManager.java`
- **实现方法**：`startDynamicRebalance(...)` (L394-467)
- **算法流程**：
  1. `scheduleAtFixedRate(..., 10_000ms)` — 每10秒执行
  2. 从 `processedByOp` 差分计算各算子 TPS
  3. 从 `cpuNanosByOp` 差分计算各算子 CPU 繁忙度
  4. 从 `stateSizeByOp` 快照计算各算子内存占用（归一化到0~1）
  5. 组装 `Map<String, OperatorInfo>`
  6. 调用 `NodeImportanceEvaluator.evaluateAndAssignRatios()` 重算 I(v)→r(v)
  7. `updateSamplingRatios(res.R)` + `updateImportance(res.I)` 热更新
- **代码位置**：`ApproxBackupManager.java:394-467`

---

## 七、系统核心机制（论文第4.1节，4.5节）

### 方法17：双轨分流机制（锚点采样对比）

- **论文描述**：SplitBolt 通过哈希散列选取约1%锚点键，将其克隆为两份分别附加真实标签（TYPE_REAL）和近似标签（TYPE_APPROX）。TYPE_REAL 在 ChaosBolt 中受保护不注入故障，TYPE_APPROX 用于全量容错
- **实现类**：`topology/SplitBolt.java`
- **锚点判定方法**：`isAnchorKey(String word)` (L26-28)
  ```java
  public static boolean isAnchorKey(String word) {
      return word != null && Math.abs(word.hashCode() % 100) == 0;
  }
  ```
  — 基于哈希空间均匀采样的 1% 选取，保证无偏性
- **双轨发射** (L54-58)：
  ```java
  if (isAnchorKey(taxiId)) {
      collector.emit(input, new Values(taxiId, TYPE_REAL));   // 真值流
  }
  collector.emit(input, new Values(taxiId, TYPE_APPROX));     // 近似流(全量)
  ```
- **ChaosBolt 保护**：`ChaosBolt.java:67-71`
  ```java
  if (type == SplitBolt.TYPE_REAL) {
      collector.emit(input, new Values(word, type));  // 直接放行，不注入故障
      collector.ack(input);
      return;
  }
  ```

### 方法18：故障注入机制

- **论文描述**：ChaosBolt 拦截 TYPE_APPROX 元组，以 failProbability 概率触发故障（发送 `FAFT_CRASH_SIGNAL`），以 delayProbability 概率注入延迟。TYPE_REAL 元组受保护不被故障注入
- **实现类**：`topology/ChaosBolt.java`
- **故障注入逻辑** (L74-83)：
  1. `random.nextDouble() < failProbability` → 故障命中
  2. `FaftLatencyMonitor.recordFailure()` — 记录故障时间
  3. `collector.emit(new Values("FAFT_CRASH_SIGNAL", type))` — 发送崩溃信号替代原始数据
  4. `collector.ack(input)` — 手动ACK，防止Spout重发
- **延迟注入** (L88-89)：
  ```java
  if (random.nextDouble() < delayProbability) {
      Thread.sleep(delay);
  }
  ```
- **故障参数配置**：
  - `FaftTopologyLauncher`: `new ChaosBolt(0.0001, 0, 50)` — 0.01%故障率，无延迟
  - `YSBTopologyLauncher`: 同上
  - `DefaultTopologyLauncher`: `new ChaosBolt(0.05, 0.05, 100)` — 5%故障率，5%延迟100ms

### 方法19：实验指标埋点

| 论文实验指标 | 埋点代码位置 | 输出格式 |
|-------------|-------------|---------|
| **I/O开销** (I/O Ops计数) | `ApproxBackupManager.tryBackup()` L186-189 | `[EXP-METRIC] Type=RESOURCE_IO_OPS TotalCount=...` |
| **故障恢复时间 RTO** | `FaftCountBolt.execute()` L112 / `WindowAggregationBolt.execute()` L84 | `[EXP-METRIC] Type=RTO Time=...ms Task=...` |
| **故障记录时刻** | `FaftLatencyMonitor.recordFailure()` L94 | `[EXP-METRIC] Faft 故障时间已记录：...` |
| **恢复耗时结果** | `FaftLatencyMonitor.checkAndRecordRecovery()` L119 | `[EXP-RESULT] Faft恢复耗时：... ms` |
| **全局 MRE 日志** | `FaftSinkBolt.calculateAndAdjustGlobalMRE()` L211-212 | `[Sink] 已处理...条结果 \| 当前全局 MRE=...%` |
| **PID 调节日志** | `ApproxBackupManager.adjustByError()` L281-282 | `[FAFT-PID] 误差过高 (MRE=...%), 触发激进调节!` |
| **采样率更新日志** | `ApproxBackupManager.updateSamplingRatios()` L136 | `[FAFT RatioUpdate] {...}` |
| **重要性更新日志** | `ApproxBackupManager.updateImportance()` L143 | `[FAFT ImportanceUpdate] {...}` |
| **分层升级日志** | `ApproxBackupManager.adjustByError()` L309/L315 | `[FAFT Adjust] ⬆️ Tier1全部满载，升级至Tier2` |

---

## 八、配置参数与论文符号对应

| application.yml | 论文符号 | 默认值 | 代码中的使用位置 |
|-----------------|---------|--------|----------------|
| `faft.alpha` | α (O(v)权重) | 0.34 | `NodeImportanceEvaluator.Weights` |
| `faft.beta` | β (D(v)权重) | 0.33 | `NodeImportanceEvaluator.Weights` |
| `faft.gamma` | γ (C(v)权重) | 0.33 | `NodeImportanceEvaluator.Weights` |
| `faft.impact-delta` | δ (衰减系数) | 0.9 | `OutputImpact.compute(dag, sinks, delta)` |
| `faft.omega-in` | ωin (入度权重) | 0.5 | `UpDownStreamDependency.compute()` |
| `faft.omega-out` | ωout (出度权重) | 0.5 | `UpDownStreamDependency.compute()` |
| `faft.rmin` | rmin (采样率下界) | 0.10 | `ApproxBackupManager.rmin` |
| `faft.rmax` | rmax (采样率上界) | 1.00 | `ApproxBackupManager.rmax` |
| `faft.step` | Δr (基础步长) | 0.05 | `ApproxBackupManager.step` |
| `faft.error-threshold` | Emax (SLA误差上限) | 0.10 | `adjustByError(Eobs, Emax)` |
| `faft.weights` | 差异化 (α,β,γ) 表 | 各算子独立配置 | `weightsObjMap` |
| `faft.ysb-weights` | YSB拓扑差异化权重 | 各算子独立配置 | `YSBTopologyLauncher` 加载 |
| `faft.zk-connect` | ZK连接地址 | 集群地址 | `FaftLatencyMonitor.setZkConnect()` |
| `Kp=2.5` (硬编码) | 比例增益系数 | 2.5 | `ApproxBackupManager.adjustByError()` L276 |
| `λ=0.5` (硬编码) | 容忍度系数 | 0.5 | `ApproxBackupManager.adjustByError()` L264 |
| `θ=30%` (硬编码) | 分层倾斜比例 | 0.3 | `ApproxBackupManager.adjustByError()` L270 |

---

## 附录：三类实验拓扑对照

| Launcher | 对应论文实验 | DAG链路 | 关键差异 |
|----------|-------------|--------|---------|
| `DefaultTopologyLauncher` | Storm默认容错基线 | SourceSpout→Split→Filter→Chaos→Count→Sink | 无FAFT机制；使用Storm内置ACK重放恢复 |
| `FaftTopologyLauncher` | Word Count拓扑实验 | FileSourceSpout→Split→Filter→Chaos→FaftCount→FaftSink | FAFT双轨+FaftCountBolt维护双视图+FaftSinkBolt计算MRE |
| `YSBTopologyLauncher` | YSB拓扑实验 | FileSourceSpout→EventFilter→Project→Split→Chaos→Join→WindowAgg→FaftSink | 更深的DAG链路；EventFilter/Project为边缘算子；Join/WindowAgg为关键算子 |
